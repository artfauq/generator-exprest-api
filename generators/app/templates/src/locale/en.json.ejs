{
  "errors": {<% if (jwt) { %>
    "invalidAuthentication": "Invalid authentication.",<% } %><% if (auth) { %>
    "invalidCredentials": "Invalid credentials.",<% } %><% if (jwt) { %>
    "invalidToken": "Invalid token.",<% } %>
    "routeNotFound": "The route '{{method}} {{baseUrl}}{{url}}' doesn't exist.",
    "tooManyRequests": "Too many requests, please try again later."
  }
}
