// Export middlewares from here

export * from './error-handler.middleware';<% if (jwt) { %>
export * from './jwt.middleware';<% } %><% if (celebrate) { %>
export * as validation from './validation.middleware';<% } %>
