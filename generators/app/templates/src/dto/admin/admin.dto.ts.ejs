import { CurrentAdmin } from 'adminjs';
import { User } from '../../models';

export const adminDTO = (user: User): CurrentAdmin => {
  const adminDto: CurrentAdmin = {
    id: user.id,
    email: user.email,
    title: user.fullName,
  };

  return adminDto;
};
