const { db<% if (mocha) { %>, testConfig<% } %> } = require('./index');

// Sequelize options
const options = {
  dialect: '<%= dialect %>',
  define: {
    charset: 'utf-8',
  },
  logging: false,
  migrationStorageTableName: 'sequelize_migrations',
};

module.exports = {
  development: {
    ...options,
    host: db.host,
    username: db.username,
    password: db.password,
    database: db.database,
  },
<% if (mocha) { -%>
  test: {
    ...options,
    host: db.host,
    username: db.username,
    password: db.password,
    database: testConfig.database,
  },
<% } -%>
  production: {
    ...options,
    host: db.host,
    username: db.username,
    password: db.password,
    database: db.database,
  },
};
