<% if (sequelize) { %>import { UserRoleEnum } from './enums';

<% } %>/**
 * Decoded JWT token
 */
export type DecodedJWT = StandardJWTClaims & JWTPayload;

/**
 * Standard JWT claims
 */
export type StandardJWTClaims = {
  iat: number;
  iss: string;
  sub: string;
  jti: string;
};

/**
 * Custom JWT claims
 */
export type JWTPayload = <% if (sequelize) { %>AuthUser;

export type AuthUser = {
  id: string;
  email: string;
  fullName: string;
  role: UserRoleEnum;
};
<% } else { %>{};
<% } %>