export type User = {
  id: string;
  fullName: string;
  email: string;
  createdAt: string;
};

export type CreateUserDTO = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
