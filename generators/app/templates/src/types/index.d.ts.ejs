// Declare here type override declarations and global types

<% if (jwt) { %>import { UserAttributes } from '../models';

declare global {
  namespace Express {
    interface Request {
      user?: UserAttributes;
    }
  }
}
<% } else { %>export = {};
<% } %>