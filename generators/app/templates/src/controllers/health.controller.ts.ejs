import Router from 'express-promise-router';

const router = Router();

/**
 * GET /health
 *
 * Check server health
 */
router.get('/', (req, res) => {
  res.sendStatus(204);
});

export default router;
