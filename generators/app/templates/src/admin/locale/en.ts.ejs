import { Locale, LocaleTranslations } from 'adminjs';

const translations: LocaleTranslations = {
  actions: {},
  buttons: {},
  labels: {
    loginWelcome: '<%= name %> | Admin',
  },
  messages: {},
  properties: {},
  resources: {},
};

const locale: Locale = {
  language: 'en',
  translations,
};

export default locale;
