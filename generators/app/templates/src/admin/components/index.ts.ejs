import { ComponentLoader } from 'adminjs';

// See https://docs.adminjs.co/ui-customization/writing-your-own-components
const componentLoader = new ComponentLoader();

const Components = {
  // Example: componentLoader.add('Example', './example.tsx'),
};

export { componentLoader, Components };
