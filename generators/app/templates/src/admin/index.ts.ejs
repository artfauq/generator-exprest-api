import { componentLoader } from './components';
import * as translations from './locale';
import * as resources from './resources';

const menu = {
  users: {
    name: 'Utilisateurs',
    icon: 'UserMultiple',
  },
};

export { componentLoader, menu, resources, translations };
