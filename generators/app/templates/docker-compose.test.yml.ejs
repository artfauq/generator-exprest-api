version: '3'

services:
  app:
    build:
      target: development
    container_name: <%= shortname %>-app-test
    entrypoint: dockerize<% if (sequelize) { %> -wait tcp://db:${DB_PORT}<% } %> -timeout 30s npm run test<% if (sequelize) { %>

  db:
    container_name: <%= shortname %>-db-test

  migrate:
    entrypoint: ['echo', 'Migration service disabled']<% } %>
