process.env.NODE_ENV = 'test';

module.exports = {
  exit: true,
  file: ['./test/setup.ts'],
  recursive: true,
  reporter: 'spec',
  require: 'ts-node/register',
  spec: './test/**/*.test.ts',
  timeout: 10000,
};
