FROM node:latest

WORKDIR /<%= shortname %>

# install packages
COPY package*.json ./
RUN npm set progress=false && npm install --production

# copy application files
COPY . /<%= shortname %>

EXPOSE 8080

CMD [ "npm", "start" ]
