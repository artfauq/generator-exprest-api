import { agent } from '../jest.setup';

/**
 * GET /sentry-debug
 */
describe('GET /sentry-debug', () => {
  it('should return 500 on sentry debug route', async () => {
    await agent.get('/sentry-debug').accept('json').expect(500);
  });
});
