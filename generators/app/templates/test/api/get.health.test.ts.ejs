import { agent } from '../jest.setup';

/**
 * GET /health
 */
describe('GET /health', () => {
  it('should return 204 on health route', async () => {
    await agent.get('/health').accept('json').expect(204);
  });
});
