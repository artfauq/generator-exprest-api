# Environment config
NODE_ENV

# Server config
PORT

# Application config
PUBLIC_HOST

<% if (sequelize) { -%>
# Database
DB_HOST
DB_PORT
DB_NAME
DB_USER
DB_PASSWORD

<% } -%><% if (jwt) { -%>
# JWT
JWT_SECRET

<% } -%><% if (nodemailer) { -%>
# SMTP
SMTP_HOST
SMTP_PORT
SMTP_USERNAME
SMTP_PASSWORD<% } -%>
